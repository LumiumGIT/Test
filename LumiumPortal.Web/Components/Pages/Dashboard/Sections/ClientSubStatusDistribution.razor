@using Lumium.Application.Common.Extensions
@using LumiumPortal.Web.Helpers

<MudPaper Elevation="1" Class="pa-4">
    <MudStack Spacing="3">
        <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
            <MudIcon Icon="@Icons.Material.Filled.Category" Color="Color.Primary" Size="Size.Medium"/>
            <MudText Typo="Typo.h6">Klijenti po podstatusu</MudText>
        </MudStack>

        <MudDivider/>

        @if (SubStatusDistribution.Any())
        {
            <MudStack Spacing="2">
                @foreach (var item in SubStatusDistribution)
                {
                    var subStatus = item.SubStatus;
                    var totalClients = SubStatusDistribution.Sum(d => d.Count);
                    var percentage = totalClients > 0 ? (item.Count * 100.0 / totalClients) : 0;

                    <MudStack Spacing="1">
                        <MudStack Row="true" Justify="Justify.SpaceBetween" AlignItems="AlignItems.Center">
                            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                                <MudIcon Icon="@Icons.Material.Filled.Label"
                                         Color="@ClientHelpers.GetSubStatusColor(subStatus)"
                                         Size="Size.Small"/>
                                <MudText Typo="Typo.body2">@item.SubStatus.GetDescription()</MudText>
                            </MudStack>
                            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                                <MudText Typo="Typo.body2" Color="Color.Default">
                                    @percentage.ToString("F0")%
                                </MudText>
                                <MudChip T="string"
                                         Size="Size.Small"
                                         Color="@ClientHelpers.GetSubStatusColor(subStatus)">
                                    @item.Count
                                </MudChip>
                            </MudStack>
                        </MudStack>
                        <MudProgressLinear Color="@ClientHelpers.GetSubStatusColor(subStatus)"
                                           Value="@percentage"
                                           Size="Size.Small"
                                           Rounded="true"/>
                    </MudStack>
                }
            </MudStack>
        }
        else
        {
            <MudPaper Elevation="0" Class="pa-6">
                <MudStack AlignItems="AlignItems.Center" Spacing="2">
                    <MudIcon Icon="@Icons.Material.Filled.DoNotDisturb"
                             Size="Size.Large"
                             Color="Color.Default"
                             Style="font-size: 36px; opacity: 0.3;"/>
                    <MudText Typo="Typo.body2" Color="Color.Default">
                        Nema posebnih podstatusa
                    </MudText>
                </MudStack>
            </MudPaper>
        }
    </MudStack>
</MudPaper>